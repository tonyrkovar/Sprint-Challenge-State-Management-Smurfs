const initialState = {
    name: '',
    age: 0,
    height: '',
    id: 0
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}